# Helper function to extract an RDS file

Helper function to extract an RDS file

## Usage

``` r
.load_resource(file)
```

## Arguments

- file:

  file to extract

## Value

the contents of the file
