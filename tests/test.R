library(MolgenisArmadillo)

# Sys.setenv("AWS_S3_ENDPOINT" = "localhost:9000")
# options("cloudyr.aws.default_region" = "")
#
# token <- get_token("http://localhost:8080")
# session <- assume_role_with_web_identity(token, "http://localhost:9000")
#
# list_workspaces("gecko", use_https = FALSE)
# list_folders(use_https = FALSE)
# list_workspaces("next", use_https = FALSE)
# delete_workspace("next", "patient", use_https = FALSE)
# delete_folder("next", use_https = FALSE)
# create_folder("next", use_https = FALSE)
# copy_workspace("diabetes", "patient", "next", use_https = FALSE)
#
# move_workspace("next", "patient", "gecko", use_https = FALSE)
